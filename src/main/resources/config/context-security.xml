<?xml version="1.0" encoding="UTF-8"?>
<beans:beans xmlns="http://www.springframework.org/schema/security"
             xmlns:beans="http://www.springframework.org/schema/beans"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="http://www.springframework.org/schema/beans
             http://www.springframework.org/schema/beans/spring-beans.xsd
             http://www.springframework.org/schema/security
             http://www.springframework.org/schema/security/spring-security.xsd">

    <!-- auto-config="true" : Spring Security 기본 로그인 폼 사용 -->
    <http auto-config="true">

        <intercept-url pattern="/login.do" access="permitAll()"></intercept-url>

        <intercept-url pattern="/**" access="isAuthenticated()"></intercept-url>
        
        <logout logout-url="/logout.do" logout-success-url="/login?logout" invalidate-session="true"></logout>
        
        <!-- CSRF 보호 비활성화 -->
        <csrf disabled="true"></csrf>

    </http>

    <authentication-manager>
        <authentication-provider>
            <user-service>
                <user name="user" password="{noop}1q2w3e" authorities="ROLE_USER"></user>
                <user name="admin" password="{noop}q1w2e3" authorities="ROLE_ADMIN"></user>
            </user-service>
        </authentication-provider>
    </authentication-manager>

</beans:beans>