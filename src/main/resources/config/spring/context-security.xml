<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	   xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	   xmlns:security="http://www.springframework.org/schema/security"
	   xmlns:context="http://www.springframework.org/schema/context"
	   xsi:schemaLocation="http://www.springframework.org/schema/security 
						   http://www.springframework.org/schema/security/spring-security.xsd 
						   http://www.springframework.org/schema/beans 
						   http://www.springframework.org/schema/beans/spring-beans.xsd 
						   http://www.springframework.org/schema/context 
						   http://www.springframework.org/schema/context/spring-context-4.3.xsd">

	<context:component-scan base-package="stu"/>
	
	<bean id="CustomAuthenticationSuccessHandler" class="stu.member.login.CustomAuthenticationSuccessHandler"/>
	
	<security:http>
    	<security:intercept-url pattern="/main.do" access="permitAll"/>
    	<security:intercept-url pattern="/mainList.do" access="permitAll"/>
    	<security:intercept-url pattern="/shop/**" access="permitAll"/>
	    <security:form-login username-parameter="MEMBER_ID" 
	    					 password-parameter="MEMBER_PASSWD" 
	    					 login-page="/loginForm.do"
	    					 login-processing-url="/loginAction"
	    					 authentication-success-handler-ref="CustomAuthenticationSuccessHandler"/>
	
		<security:csrf disabled="true"/>
	</security:http>
	
	<security:authentication-manager>
		<security:authentication-provider>
			<security:jdbc-user-service data-source-ref="dataSource" 
										users-by-username-query="select MEMBER_ID, MEMBER_PASSWD, CASE WHEN MEMBER_DELETE=0 THEN 1 ELSE 0 END as enabled FROM MEMBER WHERE MEMBER_ID=?"
										authorities-by-username-query="select MEMBER_ID, MEMBER_GRADE from MEMBER where MEMBER_ID=? "/>
		</security:authentication-provider>
	</security:authentication-manager>
</beans>